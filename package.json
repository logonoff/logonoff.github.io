{
  "name": "logonoff.github.io",
  "version": "0.0.0",
  "private": true,
  "packageManager": "yarn@4.10.3",
  "type": "module",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "yarn clean && eleventy",
    "start": "yarn clean && eleventy --serve"
  },
  "dependencies": {
    "11ty.ts": "^0.0.6",
    "@11ty/eleventy": "^3.1.2"
  }
}
